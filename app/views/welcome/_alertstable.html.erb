<table id="table">

  <thead>
    <tr>
      <th>ID</th>
      <th>Time</th>
      <th>Train</th>
      <th>Options</th>
    </tr>
  </thead>

  <tbody>
    <% @alerts.each do |alert| %>
    <tr>
      <td>Alert #<%= alert.id %></td>
      <td><%= alert.time_to_send.strftime("%I:%M") %></td>
      <td><%= alert.train.name %></td>
      <td><a class="btn" href="#"><i class="icon-edit icon-white"></i> Edit</a>
        <a class="btn btn-danger" href="#"><i class="icon-remove icon-white"></i> Delete</a>
      </td>
    </tr>
    <% end %>
    <tr><td>Add New Alert</td></tr>
  </tbody>

</table>